# Dagster workspace configuration
# Run `dagster dev` to start the local UI (http://localhost:3000)
load_from:
  - python_module:
      module_name: app.orchestration.data_transformation
      attribute: defs
